- name: Install and run Nginx
  hosts: mongodb
  #list
  tasks:
  - name: Install nginx
    ansible.builtin.yum:
      name: nginx
      state: Installed
  - name: Start nginx
    ansible.builtin.service:
      name: system
      state: service